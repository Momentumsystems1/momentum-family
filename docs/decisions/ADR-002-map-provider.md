# ADR-002 Proveedor de mapas

react-native-maps (Apple/Google) para render; rutas/geocoding por adaptador (Azure Maps preferente, OSRM/Nominatim como fallback no productivo). Nunca acoplar UI a un proveedor.
